@inherits LayoutComponentBase

<ThemeProvider />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.False" Class="auth-layout-container">
            <div class="auth-layout-inner">
                <div class="auth-logo-wrapper">
                    <AppLogo />
                </div>
                @Body
            </div>
        </MudContainer>
    </MudMainContent>
</MudLayout>

<style>
    .auth-layout-container {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, var(--wrkflo-background, #f9fafb) 0%, color-mix(in srgb, var(--wrkflo-primary, #4F46E5) 12%, white) 100%);
        padding: 2rem;
    }

    .auth-layout-inner {
        width: 100%;
        max-width: 420px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
    }

    .auth-logo-wrapper {
        margin-bottom: 0.5rem;
    }
</style>
